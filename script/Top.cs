using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Top : MonoBehaviour
{
    //float hand_size_x = 0.5f; // プレイヤーの手のサイズ(x)
    //float hand_size_y = 1.0f; // プレイヤーの手のサイズ(y)
    //float hand_size_z = 0.5f; // プレイヤーの手のサイズ(z)
    //float hand_position_x = 0.5f; // プレイヤーの場所(x)
    //float hand_position_y = 1.0f; // プレイヤーの場所(y)
    //float hand_position_z = 0.5f; // プレイヤーの場所(z)

    //public int ID;

    //public GameObject hitodePrefab;
    //public GameObject Controller;
    //public GameObject Top_ID;

    //void Start()
    //{
    //   // Top_ID = GameObject.Find("PlayerBodyPrefab");
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    ID = Controller.GetComponent<Cnrl>().ID;
    //    if (Controller.GetComponent<Cnrl>().mode_hitode == ID)
    //    {
    //        if (Input.GetKey(KeyCode.F1))
    //        {
    //            hand_size_y += 0.01f;
    //            hand_position_y = transform.position.y + 0.005f;
    //            hitodePrefab.transform.localScale = new Vector3(hand_size_x, hand_size_y, hand_size_z);
    //            hitodePrefab.transform.position = new Vector3(transform.position.x, hand_position_y, transform.position.z);
    //        }
    //    }
    //}
}
